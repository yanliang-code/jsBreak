##### 异步和单线程（chapter02\04异步和单线程.html）

###### 一、题目

1. 同步和异步的区别是什么？分别举一个同步和异步的例子
   - 同步会阻塞代码执行，异步不会
   - alert是同步，setTimeout是异步
2. 一个关于setTimeout的笔试题
   - ![2020-09-22_163451](D:\coding\jsBreak\md\pic\04\2020-09-22_163451.png)
   - 执行结果：1352 4
3. 前端使用异步的场景有哪些
   - 都需要等待，但不能卡顿
     - 定时任务：setTimeout、setInterval
     - 网络请求：ajax 请求、动态 img 加载（onload）
     - 事件绑定（触发事件时，才会执行指定代码）

###### 二、知识点

1. 什么是异步（对比同步）
   - 异步：遇到需要响应时间的，先执行下面的语句，等待响应成功后，在执行那部分代码}（14行）
     - 需要异步的情况
       - 定时任务：setTimeout、setInterval
       - 网络请求：ajax 请求、动态 img 加载（onload）
       - 事件绑定（触发事件时，才会执行指定代码）
   - 同步：会阻塞代码的执行，浏览器icon会显示loading状态（19行）
2. 前端使用异步的场景
3. 异步和单线程